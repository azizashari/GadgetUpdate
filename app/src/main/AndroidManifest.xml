<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="azizashari.gadgetupdate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/title_activity_splash"
            android:theme="@style/SplashShow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SmartphoneActivity" />
        <activity android:name=".NotebookActivity" />
        <activity android:name=".DroneActivity" />
        <activity android:name=".WatchActivity" />
        <activity android:name=".Actioncam" />
        <activity android:name=".CreditActivity"></activity>
    </application>

</manifest>